#variable
x = 5
print(x)

#Assignment operators
x += 3
print(x)

#variable naming conventions
#1. Variable names can only contain letters, numbers, and underscores.
#2. Variable names must start with a letter or an underscore.
#3. Variable names are case-sensitive.
#4. Variable names should not be a reserved keyword in Python.
#5. Variable names should be descriptive and meaningful.

#Comments  that explain the code and we use # to write comments in python

#Single line comment is used to explain a single line of code. example: # This is a single line comment

#Multi-line comment is used to explain multiple lines of code. example:
"""
 This is a multi-line comment
"""

#Print method is used to display output on the console. example: print("Hello, World!")

#Integer is a whole number without a decimal point. example :
x = 5

# Float is a number with a decimal point. example :
y = 3.14

print(x)
print(y)

# String is a sequence of characters enclosed in single or double quotes. example :
name = "Cedrick NTWARI"
print(name)

# Boolean is a data type that can have one of two values: True or False. example :
is_student = True
print(is_student)

# Set is an unordered collection of unique elements. example :
my_set = {1, 1, 1}
print(my_set)

# Dictionary is a collection of key-value pairs. example :
my_dict = {"name": "Cedrick" , "age": 25}
print(my_dict)

# Tuple is an ordered collection of elements enclosed in parentheses. example :
my_tuple = (1, 2, 3)
print(my_tuple)

# Range is a sequence of numbers generated by the range() function. example :
my_range = range(5)
print(my_range)

# List is an ordered collection of elements enclosed in square brackets. example :
my_list = [1, 2, 3, 4, 5]
print(my_list)

# None is a special value that represents the absence of a value or a null value. example :
my_variable = None
print(my_variable)

# Immutable data types are data types that cannot be changed after they are created. example : int, float, str, tuple, frozenset  because they cannot be modified after they are created. but we can create a new object with a different value. example :
x = 5
x = 10
print(x)

# mutable data types are data types that can be changed after they are created. example : list, set, dict because they can be modified after they are created. example :
my_list = [1, 2, 3]
print(my_list)
my_list[0] = 10
print(my_list)

# type() function is used to check the data type of a variable. example :
x = 5
print(type(x))

# isinstance() function is used to check if a variable is an instance of a specific data type. example :
x = 5
print(isinstance(x, int))

# String indexing is used to access individual characters in a string. example :
name = "Cedrick"
print(name[0]) # C 

# String escape sequences are used to represent special characters in a string. example :
name = "Cedrick\nNTWARI"
print(name)

# String concatenation is used to combine two or more strings. example :
first_name = "Cedrick"
last_name = "NTWARI"
full_name = first_name + " " + last_name
print(full_name)

Artist = "Re"
Artist += "ma"
print(Artist)

# f-strings are used to format strings in Python. example :
name = "Cedrick"
age = 25
print(f"My name is {name} and I am {age} years old.")

# String slicing is used to access a range of characters in a string. example :
name = "Cedrick"
print(name[0:3]) # Ced

# len() function is used to get the length of a string. example :
name = "Cedrick"
print(len(name)) # 7

# in operator is used to check if a substring exists in a string. example :
name = "Cedrick"
print("Ced" in name) # True

# Upper() method and lower() method are used to convert a string to uppercase and lowercase respectively. example :
name = "Cedrick"
print(name.upper()) # CEDRICK
print(name.lower()) # cedrick

# strip() method is used to remove whitespace from the beginning and end of a string. example :
name = "   Cedrick   "
print(name.strip()) # Cedrick 

# replace() method is used to replace a substring with another substring in a string. example :
name = "Cedrick"
print(name.replace("Ced", "Re")) # Rerick  

# split() method is used to split a string into a list of substrings based on a delimiter. example :
name = "Cedrick NTWARI Yannick"
print(name.split(" ")) # ['Cedrick', 'NTWARI', 'Yannick']

# join() method is used to join a list of strings into a single string with a specified delimiter. example :
name_list = ["Cedrick", "NTWARI", "Yannick"]
print(" ".join(name_list)) # Cedrick NTWARI Yannick

# startswith() method is used to check if a string starts with a specified substring. example :
name = "Cedrick"
print(name.startswith("Ced")) # True

# endswith() method is used to check if a string ends with a specified substring. example :
name = "Cedrick"
print(name.endswith("rick")) # True

# find() method is used to find the index of the first occurrence of a substring in a string. example :
name = "Cedrick"
print(name.find("rick")) # 3

# count() method is used to count the number of occurrences of a substring in a string. example :
name = "Cedrick"
print(name.count("Ced")) # 1

# capitalize() method is used to capitalize the first character of a string. example :
name = "cedrick"
print(name.capitalize()) # Cedrick

# is upper() and is lower() methods are used to check if a string is in uppercase or lowercase respectively. example :
name = "CEDRICK"
print(name.isupper()) # True
name = "cedrick"
print(name.islower()) # True

# title() method is used to capitalize the first character of each word in a string. example :
name = "cedrick ntwari"
print(name.title()) # Cedrick Ntwari

# maketrans() method is used to create a translation table for replacing characters in a string. example :
original_string = "Rebrian"
translation_table = str.maketrans("Rebrian", "Cedrick")
print(original_string.translate(translation_table)) # Cedrick

# adding ,substrings ,multiplying , division, modulo , floor division, exponentiation, and parentheses in python are used to perform arithmetic operations on numbers. example :
x = 5
y = 3
print(x + y) # 8
print(x - y) # 2
print(x * y) # 15
print(x / y) # 1.6666666666666667
print(x % y) # 2
print(x // y) # 1
print(x ** y) # 125

# floor() method is used to round a number down to the nearest integer. example :
import math 
print(math.floor(3.7)) # 3

# int() function is used to convert a number or a string to an integer. example :
x = 5.7
print(int(x)) # 5  

# round() function is used to round a number to the nearest integer. example :
x = 5.7
print(round(x)) # 6

# abs() function is used to get the absolute value of a number. example :
x = -5
print(abs(x)) # 5

# pow() function is used to calculate the power of a number. example :
x = 5
y = 3
print(pow(x, y)) # 125

# Augmented assignment operators are used to perform an operation and assign the result to a variable in a single step. example :
x = 5
x += 3 # equivalent to x = x + 3
print(x) # 8
y = 5
y -= 3 # equivalent to y = y - 3
print(y) # 2
z = 5
z *= 3 # equivalent to z = z * 3
print(z) # 15
a = 5
a /= 3 # equivalent to a = a / 3
print(a) # 1.6666666666666667
b = 5
b %= 3 # equivalent to b = b % 3
print(b) # 2
c = 5
c //= 3 # equivalent to c = c // 3
print(c) # 1
d = 5
d **= 3 # equivalent to d = d ** 3
print(d) # 125
i = 5
i &= 3 # equivalent to i = i & 3
print(i) # 1
j = 5
j |= 3 # equivalent to j = j | 3
print(j) # 7
k = 5
k ^= 3 # equivalent to k = k ^ 3
print(k) # 6
h = 5
h >>= 3 # equivalent to h = h >> 3
print(h) # 0
l = 5
l <<= 3 # equivalent to l = l << 3
print(l) # 40

# function is a block of code that performs a specific task and can be reused multiple times. example :
def greet(name):
    print(f"Hello, {name}!")
greet("Cedrick") # Hello, Cedrick!

# def is used to define a function in Python. example :
def adding(x, y):
    return x + y
result = adding(5, 3)
print(result) # 8

# function parameters are the values that are passed to a function when it is called. example :
def salutation(name):
    print(f"Hello, {name}!")
salutation("Cedrick") # Hello, Cedrick!

# function arguments are the values that are passed to a function when it is called. example :
def multiply(x, y):
    return x * y
result = multiply(5, 3)
print(result) # 15

# return statement is used to return a value from a function. example :
def subtract(x, y):
    return x - y
result = subtract(5, 3)
print(result) # 2

# Default values for function parameters are used to provide a default value for a parameter if no value is provided when the function is called. example :
def Default(name="Guest"):
    print(f"Hello, {name}!")
Default() # Hello, Guest!
Default("Cedrick") # Hello, Cedrick!

# TypeError is raised when an operation or function is applied to an object of inappropriate type. example :
def add(x, y):
    return x + y
result = add(5, 3) # This will work correctly
print(result) # 8

# TypeError is raised when an operation or function is applied to an object of inappropriate type. example :
"""
result = add(5, "3") # TypeError: unsupported operand type(s) for +: 'int' and 'str'   

"""

# input() function is used to get user input from the console. example :
name = input("Enter your name: ")
print(f"Hello, {name}!") # Hello, Cedrick!

# local variable is a variable that is defined inside a function and can only be accessed within that function. example :
def local_variable():
    x = 5
    print(x) # 5
local_variable()

# global variable is a variable that is defined outside of any function and can be accessed from anywhere in the code. example :
x = 5
def global_variable():
    print(x) # 5
global_variable()

# global keyword is used to declare a variable as global inside a function. example :
x = 5
def global_keyword():
    global x
    x = 10
    print(x) # 10
global_keyword()

# Enclosing scope is the scope of a variable that is defined inside a nested function. example :
def outer_function():
    x = 5
    def inner_function():
        print(x) # 5
    inner_function()
outer_function()

# Built-in scope is the scope of a variable that is defined in the built-in namespace. example :
print(len("Cedrick")) # 7

# comparison operators are used to compare two values and return a boolean value (True or False). example :
x = 5
y = 3
print(x == y) # False
print(x != y) # True
print(x > y) # True
print(x < y) # False
print(x >= y) # True
print(x <= y) # False

# if statement is used to execute a block of code if a condition is true. example :
x = 5
if x > 3:
    print("x is greater than 3") # x is greater than 3

# if-else statement is used to execute a block of code if a condition is true and another block of code if the condition is false. example :
x = 5 
if x > 3:
    print("x is greater than 3") # x is greater than 3
else:
    print("x is not greater than 3")

# elif statement is used to execute a block of code if a condition is true and another block of code if the condition is false. example :
x = 5
if x > 3:
    print("x is greater than 3") # x is greater than 3
elif x == 3:
    print("x is equal to 3")
else:
    print("x is less than 3")

# nested if statement is used to execute a block of code if a condition is true and another block of code if the condition is false. example :
x = 5
if x > 3:
    if x < 10:
        print("x is greater than 3 and less than 10") # x is greater than 3 and less than 10
    else:
        print("x is greater than or equal to 10")

# truthy values are values that evaluate to True in a boolean context. example : non-zero numbers, non-empty strings, non-empty lists, non-empty tuples, non-empty dictionaries, and non-empty sets are all truthy values. example :    
if x:
    print("x is truthy") # x is truthy

# falsy values are values that evaluate to False in a boolean context. example : 0, None, False, empty strings, empty lists, empty tuples, empty dictionaries, and empty sets are all falsy values. example :
x = 0
if not x:
    print("x is falsy") # x is falsy 

# bool () function is used to convert a value to a boolean value. example :
x = 5
print(bool(x)) # True

# boolean operators are used to combine boolean values and return a boolean value. example :
x = True
y = False
print(x and y) # False
print(x or y) # True
print(not x) # False  

# and operator is used to combine two boolean values and return True if both values are True. example :
x = True
y = False
print(x and y) # False

# or operator is used to combine two boolean values and return True if at least one value is True. example :
x = True
y = False
print(x or y) # True

# Short-circuit evaluation is a programming technique used to optimize the evaluation of boolean expressions. example :
x = True
y = False
if x or y:
    print("At least one value is True") # At least one value is True

# not operator is used to negate a boolean value and return the opposite value. example :
x = True
print(not x) # False

def greetf():
    pass
    
print(greetf())

message = 'Python is fun!'

print(message[0:6]) 

example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # example dashed name

print("Hello, world!") 